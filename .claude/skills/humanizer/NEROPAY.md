# humanizer — NeroPay house notes

Installed 24 Sep 2026 at Faisal's request from blader/humanizer (MIT, v3.0.0). Instructions only; no code runs.

- Use it to **rewrite**. `/slopmonster` stays the pass/fail gate that CLAUDE.md requires on every caption:
  humanizer first, then SlopMonster's score.
- The rails in `CLAUDE.md` win wherever the two disagree. Humanizer already forbids inventing facts, names or
  numbers, which matches rail 5 and the figures register.
- NeroPay voice still applies: "we", never "I"; no emoji; no hashtags; a concession in every educational post.
